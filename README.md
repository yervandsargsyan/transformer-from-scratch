# transformer-from-scratch
Pure PyTorch GPT-style Transformer from scratch with byte-level BPE tokenizer

A minimal GPT-style Transformer implemented from scratch in PyTorch, 
including a byte-level BPE tokenizer, causal self-attention, 
training loop, and autoregressive text generation. 

